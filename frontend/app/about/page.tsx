import { aboutMetadata } from '@/lib/marketing-seo';
import { AboutContent } from '@/components/marketing/about-content';

export const metadata = aboutMetadata;

export default function AboutPage() {
  return <AboutContent />;
}
