import { bookDemoMetadata } from '@/lib/marketing-seo';

export const metadata = bookDemoMetadata;

export default function BookDemoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
