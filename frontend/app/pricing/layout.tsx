import { pricingMetadata } from '@/lib/marketing-seo';

export const metadata = pricingMetadata;

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
